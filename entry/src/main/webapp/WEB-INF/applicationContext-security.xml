<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- 认证成功后的处理类 -->
    <beans:bean id="authSuccess" class="com.james.system.security.AuthenticationSuccessHandlerImpl"/>

    <http auto-config="true">
        <form-login username-parameter="username" password-parameter="password"
                    login-page="/login.jsp"
                    login-processing-url="/login.do"
                    authentication-success-handler-ref="authSuccess"
                    authentication-failure-url="/login.jsp?error=login_failure"/>
        <intercept-url pattern="/rest/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/**/*.do" access="hasRole('ROLE_USER')" />
        <logout logout-url="/logout.do"  invalidate-session="true" logout-success-url="/login.jsp"  />
    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_ADMIN"/>
                <user name="james" password="james" authorities="ROLE_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>


</beans:beans>